# Development

> **Purpose:** Record repository-specific validation constraints, CI behavior, generation, native dependencies, and stale guidance.
> **Last verified:** 2026-09-18
> **Verified against commit:** `c914b4cc84f87197c03009bafefc06f3f513700b`
> **Relevant source paths:** `AGENTS.md`, `go.mod`, `.github/workflows/`, `.golangci.yaml`, `ci/Dockerfile`, `swagger.sh`, `coverage.sh`, `CONTRIBUTING.md`

## Repository Constraint

`AGENTS.md` says the build system is not installed and agents must not build or run tests unless the task specifically requires it. Follow that instruction over generic Go workflows.

The module declares Go 1.25.0 (`go.mod`). The root `main.go` exists only so package patterns such as `go test ./...` include the repository root.

## Validation Present in the Repository

| Check | Repository evidence |
|---|---|
| PR tests | `.github/workflows/test.yml` uses the SeaSearch CI image and runs `go test ./...` |
| PR lint | `.github/workflows/lint.yml` runs golangci-lint; `.golangci.yaml` enables `govet`, `staticcheck`, and `gofmt` formatting |
| Coverage helper | `coverage.sh` runs race/atomic coverage and enforces 81%; it is not the current PR test command |
| Development reload | `develop.sh` runs `reflex ... go run cmd/zincsearch/main.go`; requires external `reflex` |
| Swagger generation | `swagger.sh` requires external `swag` and runs `swag init -g cmd/zincsearch/main.go` |

There is no Makefile, Taskfile, general build orchestrator, root application Dockerfile, or `test.sh` in the current tree.

## Native Build Requirements

The current source requires CGO/native libraries without build tags:

- `faiss_wrapper/faiss.go` links `-lfaiss`; `faiss_wrapper/faiss_wrapper.cc` uses FAISS C++ APIs.
- `pkg/core/ivfpq_segment.go` uses FAISS bindings.
- `pkg/core/hnsw_segment.go` uses the USearch Go/native binding.
- `pkg/core/vec_index_manager.go` imports `C`.
- `go.mod` pins USearch commit `29e5277` to native USearch v2.25.2.

`ci/Dockerfile` installs FAISS, USearch, OpenBLAS, CMake, and C++ tooling. This CI image is the clearest executable dependency specification. The old `CONTRIBUTING.md` suggestion to use `CGO_ENABLED=0` is incompatible with the current tree.

## Generated and Data Files

Generated files, do not manually edit:

- `docs/docs.go`, `docs/swagger.json`, `docs/swagger.yaml`: generated by `swagger.sh`; `docs/docs.go` is explicitly marked generated.

Embedded data assets:

- `pkg/bluge/analysis/lang/chs/gse_dict.go` and `gse_stop.go` contain large GSE dictionary/stop-word literals whose provenance is described in `pkg/bluge/analysis/lang/chs/README.md`. No repository regeneration workflow was verified; preserve provenance and verify the intended update method before changing them.

Swagger is annotation-driven and does not fully cover runtime routes. Change route/handler annotations as needed, then regenerate; validate against `pkg/routes/routes.go` `SetRoutes`.

## Guidance Drift

Treat `CONTRIBUTING.md`, `README-zincsearch.md`, and `test/README.md` cautiously:

- `CONTRIBUTING.md` references older Go versions, a missing `web/` UI, missing Dockerfiles, and a missing UI lint script.
- UI serving is commented out in `pkg/routes/routes.go` and there is no `web/` directory.
- `test/README.md` references a missing `test.sh` and old repository commands.
- Current process scope is defined by `AGENTS.md`, current dependencies by `go.mod`/`ci/Dockerfile`, and current CI by `.github/workflows/`.

## Change Checklist

1. Read `AGENTS.md` and the relevant system-map documents.
2. Inspect the exact source paths/symbols cited by the map; implementation is authoritative.
3. Keep application changes small and account for all three process routers/configs where relevant.
4. Update generated Swagger only through `swagger.sh` when annotations/API docs change.
5. Update affected `docs/system-map/` documents in the same change for architecture, process, flow, API, storage, or configuration changes.
6. Run build/tests only when specifically required and in an environment with the native dependencies.
